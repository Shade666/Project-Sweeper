<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="PKHL.ProjectSweeper.Controls.FilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="25" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Header}"
                   VerticalAlignment="Center" Margin="0,0,5,0"/>

        <TextBox Grid.Column="1" Name="FilterTextBox"
                 VerticalAlignment="Stretch"
                 TextChanged="FilterTextBox_TextChanged"
                 VerticalContentAlignment="Center"/>

        <Button Grid.Column="2" Content="X" Width="20" Height="20"
                Margin="2,0,0,0"
                Click="ClearButton_Click"
                ToolTip="Clear filter"/>
    </Grid>
</UserControl>
